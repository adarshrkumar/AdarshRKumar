---
// Imports
import '../styles/components/large-frame.scss'
import type { LargeFrameProps } from '../lib/types'

import { extractChannelId, extractVideoId, generateYouTubeImageUrls, generateYouTubeUrls } from '../lib/youtube'

// Component props and data processing
// Get component props
const { video, feed } = Astro.props

// Extract IDs and generate URLs
const channelId = extractChannelId(feed.link)
const videoId = extractVideoId(video.guid)
const imageUrls = await generateYouTubeImageUrls(videoId)
const youtubeUrls = generateYouTubeUrls(videoId, channelId)

// Generate alt text and titles
const imageAlt = `YouTube Video Thumbnail for "${video.title}" uploaded by "${video.author}"`
const frameTitle = `"${video.title}" uploaded by "${video.author}"`

---

<!-- Large frame component -->
<label class="large-frame">
    <input type="checkbox" class="large-frame__checker" />
    <img
        class="large-frame__img"
        src={imageUrls.highRes}
        alt={imageAlt}
    />
    <div class="large-frame__info">
        <h2 class="title">{video.title}</h2>
        <p class="author">
            by <a target="_blank" href={youtubeUrls.channel}>{feed.title}</a>
        </p>
    </div>
    <iframe
        class="large-frame__frame"
        allow="autoplay; fullscreen;"
        loading="lazy"
        src={youtubeUrls.embed}
        title={frameTitle}
    />
</label>
